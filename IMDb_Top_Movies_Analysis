--Dataset Description:
--The dataset contains information about the best movies on IMDb, including details such as movie name, revenue, IMDb score, genre, 
--release year, overview, crew, status, country of release, and language.

--Let's start by Retrieving all columns and rows from the Top_Movies table 
SELECT * 
FROM Top_Movies;

--What are the top 10 movies based on their IMDb score?
SELECT TOP 10 names, score
FROM Top_Movies
ORDER BY score DESC;

--Which is the movie with the highest budget?
SELECT TOP 1 names, budget_x
FROM Top_Movies
ORDER BY budget_x DESC;

--Which languages have the highest-rated movies on average?
SELECT orig_lang, AVG(score) AS avg_score
FROM Top_Movies
GROUP BY orig_lang
ORDER BY avg_score DESC;

--Which year had the most movies released?
SELECT TOP 1 date_x, COUNT(*) AS movie_count
FROM Top_Movies
GROUP BY date_x
ORDER BY COUNT(*) DESC;

-- Selects the top 10 movies with the highest revuenes in our dataset
SELECT TOP 10 names, revenue
FROM Top_Movies
ORDER BY revenue DESC;

-- What is the total revenue generated by movies from each country?
SELECT country, AVG(revenue) AS Total_revenue
FROM Top_Movies
GROUP BY country;

--What are the top 5 genres with the highest average revenue?
SELECT TOP 10 genre, AVG(revenue) AS avg_revenue
FROM Top_Movies
GROUP BY genre
ORDER BY avg_revenue DESC;

--How many movies were released in each genre?
SELECT genre, COUNT(*) AS num_movies
FROM Top_Movies
GROUP BY genre
ORDER BY num_movies DESC;

--Average revenue and score per genre
SELECT genre, AVG(revenue) AS avg_revenue, AVG(score) AS avg_score
FROM Top_Movies
GROUP BY genre
ORDER BY avg_score DESC;
